checks for report_customer_segments:
  - schema:
      fail:
        when required column missing: [country, iso, total_quantity, total_revenue_gbp]
  - row_count > 0
  - missing_count(country) = 0:
      name: All rows have a country
